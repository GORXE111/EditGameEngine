#pragma once
#include <string>

// Farm world: grid, crops, robot, economy, tick loop, native robot API.
// Engine-independent. M2 fills this in.
namespace farm::sim {

std::string version();

}  // namespace farm::sim
