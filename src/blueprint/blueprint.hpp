#pragma once
#include <string>

// Node-graph model + AST<->graph codec + layout persistence.
// Engine-independent. M4/M5 fill this in.
namespace farm::blueprint {

std::string version();

}  // namespace farm::blueprint
